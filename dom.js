// Some dom commands

let heading = document.getElementById("heading");
console.dir(heading);

let repeatedButton = document.getElementsByClassName("repeatedButton");
console.dir(repeatedButton);

let paras = document.getElementsByTagName("p");
console.dir(paras);

//query selector
let firstElement = document.querySelector("p");
console.dir(firstElement);

let allElement = document.querySelectorAll("p");
console.dir(allElement);

//Properties
/*
heading.tagName;
heading.innerText;
heading.innerHTML;
heading.content;

 */
